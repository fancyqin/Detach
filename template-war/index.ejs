<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>ejs</title>
</head>
<body>
<h2>engine: <%= engine %></h2>

<ul>
    <% for (var i=0;i<items.length;i++){ %>
        <li>
            <table width="100%" class="table" border="1" cellspacing="0" cellpadding="0">
                <tr>
                    <th width="5%">Index</th>
                    <th width="10%">ID</th>
                    <th width="15%">Name</th>
                    <th width="10%">Stars</th>
                    <th width="10%">Forks</th>
                    <th>Description</th>
                </tr>
                <tr>
                    <td><%= i + 1 %></td>
                    <td><%= items[i].id%></td>
                    <td><a href="<%= items[i].html_url%>"><%= items[i].name%></a></td>
                    <td><%= items[i].stargazers_count%></td>
                    <td><%= items[i].forks%></td>
                    <td><%= items[i].description%></td>
                </tr>
            </table>
        </li>
    <% } %>
</ul>
</body>
</html>